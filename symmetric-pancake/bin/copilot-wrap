#!/usr/bin/env bash
# copilot-wrap launcher (macOS / Linux). Generates & opens your Copilot Wrapped.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$(command -v python3 || command -v python)"
exec "$PY" "$HERE/../scripts/wrap.py" "$@"
